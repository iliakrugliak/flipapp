'use client'
import { updateVenue } from '@/app/actions'

export default function VenueProfile({ venue }) {
  // Реализация аналогична вашему текущему компоненту,
  // но использует переданный venue вместо контекста
  
  const handleUpdate = async (data) => {
    await updateVenue(venue.id, data)
    // Обновляем данные
  }

  return (
    <div className="max-w-md mx-auto p-4">
      <h1 className="text-2xl font-bold">{venue.name}</h1>
      {/* Остальной интерфейс */}
    </div>
  )
}